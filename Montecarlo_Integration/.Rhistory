installed.packages("ggplot2")
installe.packages("ggplot2")
install.packages("ggplot2")
install.packages("shiny")
- Enfrentamos complejidad con complejidad
of the degree of <span class="blue3">reproducibility</span> of the patterns and regularities within data.
Thus, by observing patterns and regularities in one data set, we wish to predict
what patterns and regularities will exist in another, *statistically similar data
set*. Predictability varies as a function of a problem’s “features” and “feature
values”, i.e., the predictor variables in a problem and their values. For instance,
one might find that income is a weak predictor of car ownership but a strong
options(repos="cran.itam.mx")
install.packages("R2OpenBUGS")
install.packages("R2jags")
library(R2OpenBUGS)
library(R2OpenBUGS)
library(R2jags)
n<-100
credito<-c(rep(1,n/2),rep(0,n/2))
data<-list("n"=n,"x"=credito)
#-Defining inits-
inits<-function(){list(theta=0.5,x1=rep(1,2))}
inits<-function(){list(lambda=0)}
#inits<-function(){list(theta=0.5,eta=1)}
#-Selecting parameters to monitor-
parameters<-c("theta","x1")
#parameters<-c("theta","eta")
#-Running code-
#OpenBUGS
ej1.sim<-bugs(data,inits,parameters,model.file="Ej1.txt",
n.iter=5000,n.chains=1,n.burnin=500)
library(R2jags)
install.packages("R2OpenBUGS")
install.packages("R2OpenBUGS")
shiny::runApp('Dropbox/Data Science/Estadistica Computacional/compstat2016/Montecarlo_Integration')
runApp('Dropbox/Data Science/Estadistica Computacional/compstat2016/Montecarlo_Integration')
